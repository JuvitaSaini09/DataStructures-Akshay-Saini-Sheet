/**
 * Problem: Reverse Integer (LeetCode 7)
 *
 * Given a signed 32-bit integer `x`, return the integer formed by
 * reversing its digits.
 *
 * If reversing `x` causes the value to go outside the signed
 * 32-bit integer range [-2^31, 2^31 - 1], return 0.
 *
 * The environment does NOT allow storing 64-bit integers.
 *
 * Examples:
 *
 * Input: 123
 * Output: 321
 *
 * Input: -123
 * Output: -321
 *
 * Input: 120
 * Output: 21
 *
 * Requirements:
 * • Preserve the sign of the number.
 * • Remove leading zeros after reversal.
 * • Return 0 if the reversed integer overflows.
 *
 * Constraints:
 * • -2^31 <= x <= 2^31 - 1
 */

/**
 * @param {number} x
 * @return {number}
 */
var reverse = function(x) {
    
};